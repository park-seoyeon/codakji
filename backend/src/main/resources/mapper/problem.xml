<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.ssafy.codackji.model.mapper.ProblemMapper">

	<select id="allProblem" resultType="ProblemDto">
		select problem_rank, admin_user_number,problem_title, problem_content, problem_group, problem_rank,
		problem_date, created_at, updated_at, problem_input, problem_output, problem_number
		from Problem
	</select>
	
	<select id="listProblem" parameterType="int" resultType="ProblemDto">
		select problem_rank, admin_user_number,problem_title, problem_content, problem_group, problem_rank,
		problem_date, created_at, updated_at, problem_input, problem_output, problem_number
		from Problem
		where problem_rank = #{problem_rank}
	</select>
	
	<select id="getProblem" parameterType="int" resultType="ProblemDto">
		select problem_rank, admin_user_number,problem_title, problem_content, problem_group, problem_rank,
		problem_date, created_at, updated_at, problem_input, problem_output, problem_number
		from Problem
		where problem_number = #{problem_number}
	</select> 
	
</mapper>